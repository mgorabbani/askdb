# askdb environment configuration
# Copy to .env and fill in real values for production.

# ── Server ──────────────────────────────────────────
PORT=3100
NODE_ENV=development

# ── MCP server ──────────────────────────────────────
MCP_PORT=3001

# ── Database ────────────────────────────────────────
# SQLite file path. Relative paths resolve from repo root.
DATABASE_PATH=./data/askdb.db

# ── Auth (better-auth) ──────────────────────────────
# Generate a random 64-char string for production:
#   openssl rand -hex 32
BETTER_AUTH_SECRET=dev-secret-change-in-production-please-use-random-64-chars
BETTER_AUTH_URL=http://localhost:3100

# ── Encryption ──────────────────────────────────────
# 64 hex characters (32 bytes) — used for AES-256-GCM of stored connection strings.
# Generate:
#   openssl rand -hex 32
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ── UI mode (advanced) ──────────────────────────────
# Dev: pnpm dev sets UI_DEV_MIDDLEWARE=1 automatically.
# Prod: pnpm start sets SERVE_UI=1 automatically. Don't set both.
# UI_DEV_MIDDLEWARE=1
# SERVE_UI=1
