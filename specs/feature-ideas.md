can I instead of dumping db into sandboxing, block ai to get access to fileds that user doesn't wants ai to acess? also how that yc based company offer near real time db query while maintaining data safety?

- also another idea is salesforce

- am thinking of a direction where its a integrated self app, where db can be sandboxed into a secured place only accessed by user. then they can query or do whatever they want in a native mobile app

also i am different than https://retool.com/ maybe i am not? so i will lose to them?

- code mode idea (from tanstack ai blog, apr 2026 — same pattern cloudflare/anthropic have been pushing): add execute_typescript as a 5th mcp tool. ai writes a tiny ts program that calls external_find / external_aggregate / external_count / external_distinct, runs server-side in a sandbox (v8 isolate or quickjs-wasm), returns one structured answer. kills the n+1 problem (no more 50 round trips for "average rating of top 5 products") and the math problem (averages computed in js instead of predicted by the llm). big token win too — 500 docs live and die in the sandbox, only the final number ships back to the model. field stripping still works automatically because external_* are thin wrappers around the existing MongoAdapter, so the security boundary doesn't move and every spec.md invariant still holds. stays passive — no in-app chat, no byok llm keys, claude desktop / cursor / chatgpt still drive it via mcp. real differentiator vs dbhub since their workbench is human-writes-sql, not ai-writes-typescript. open questions: keep the 4 typed tools alongside execute_typescript or replace them (probably keep — trivial queries shouldn't pay isolate boot cost)? isolated-vm (fast, native c++ addon = docker pain) vs quickjs-wasm (slower, ships anywhere with zero compile)? use tanstack ai's code-mode package or roll our own ~300 loc (tanstack ai is 5 days old, leaning roll-our-own)? skills/persistent-code-library — interesting but hard over mcp because tool list is mostly static, save for v2.
